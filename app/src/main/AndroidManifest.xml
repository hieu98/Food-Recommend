<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodrecommend">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodRecommend.Fullscreen">
        <activity android:name=".ListRecipeActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.FoodRecommend.NoActionBar.Fullscreen" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/Theme.FoodRecommend.NoActionBar.Fullscreen" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.FoodRecommend.NoActionBar.Fullscreen" />
        <activity android:name=".CongThucActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".LoadingActivity"
            android:theme="@style/Theme.FoodRecommend.NoActionBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>